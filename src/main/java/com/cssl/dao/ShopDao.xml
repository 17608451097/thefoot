<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cssl.dao.ShopDao">
	
	<select id="getShop" resultType="map">
		SELECT `shopid`,`shopname`,`price`,`Oriprice`,`prodetails`,`proreview`,gt.`grsonname`,`filename`,`Stock`
        FROM `shop` sp
        INNER JOIN `grsontype` gt ON sp.`grsonid` = gt.`grsonid`
	</select>
	
	<insert id="addproduct">
		INSERT INTO shop (`shopname`,`price`,`Oriprice`,`prodetails`,`proreview`,`grsonid`,`filename`) VALUES (#{shopname},#{price},#{oriprice},#{prodetails},#{proreview},#{grsonid},#{filename})
	</insert>
	
	<insert id="addshopstock">
		INSERT INTO `shopstock` (shopid,colorid,sizeid,stock) VALUES (#{shopid},#{colorid},#{sizeid},#{stock})
	</insert>
	
	<select id="getaddshopid" resultType="int">
		select @@IDENTITY
	</select>
	
</mapper>